# ============================================
# MASTER SAAS MUSE TEMPLATE - ENVIRONMENT CONFIGURATION
# ============================================
# For local development: Copy to .env.local
# For Vercel: Add in Project Settings > Environment Variables
# ============================================

# ==================== SUPABASE (Required) ====================
# Get from: Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ==================== STRIPE (Required for payments) ====================
# Get from: Stripe Dashboard > Developers > API keys
STRIPE_SECRET_KEY=sk_test_xxx
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
# Get from: Stripe Dashboard > Developers > Webhooks > Your endpoint
STRIPE_WEBHOOK_SECRET=whsec_xxx

# ==================== EMAIL - RESEND (Required) ====================
# Get from: Resend Dashboard > API Keys
RESEND_API_KEY=re_xxx
RESEND_FROM_EMAIL=noreply@yourdomain.com

# ==================== ERROR TRACKING - SENTRY (Deferred) ====================
# NOTE: Sentry integration deferred until @sentry/nextjs supports Next.js 16
# Keep these ready for when Sentry releases compatibility
# Get from: Sentry Project Settings > Client Keys (DSN)
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# Get from: Sentry Settings > Organization Settings
SENTRY_ORG=your-org-slug
SENTRY_PROJECT=your-project-slug

# ==================== ANALYTICS - PLAUSIBLE (Recommended) ====================
# Your domain as registered in Plausible
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=yourdomain.com

# ==================== APPLICATION ====================
# Your production URL (used for OAuth redirects, emails, etc.)
NEXT_PUBLIC_APP_URL=https://yourdomain.com

# ==================== GOOGLE OAUTH (Optional) ====================
# Configure in Supabase Dashboard > Authentication > Providers > Google
# These are stored in Supabase, not in your app's environment
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# ============================================
# QUICK START CHECKLIST
# ============================================
# [ ] Create Supabase project, copy URL and keys
# [ ] Create 'avatars' storage bucket with RLS policies
# [ ] Run Supabase migrations for user_roles, organization_settings, audit_logs
# [ ] Create Stripe account, copy API keys
# [ ] Create Stripe webhook pointing to /api/stripe/webhook
# [ ] Create Resend account, copy API key
# [ ] (Optional) Create Sentry project, copy DSN
# [ ] (Optional) Create Plausible account, add domain
# [ ] Deploy to Vercel with environment variables
# [ ] Bootstrap first admin user
# ============================================